Current data structure:

BE:

enviornment: uv
framework: flask
    Blueprints -> BE templating
language: python
database: sqlite3 # volitile database needs replacement AND upgrade to flask-sqlalchemy for schema.
auth: 


UI:
Jinja2 front end-templating??
css        - style
javascript - interactivity

// flask --app taskr run --debug

// flask --app taskr init-db